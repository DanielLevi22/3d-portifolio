export interface Experience {
  title: string;
  company_name: string;
  icon: string;  // ou o tipo correto para a imagem, se for um import de arquivo de imagem
  iconBg: string;
  date: string;
  points: string[];
}